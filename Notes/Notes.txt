GitBash {
	git add -A
	git commit -m "/message/"
	git push origin main
}

Connector {
	Corridor Connector {
		Use greedy algorithm to get as close as possible to the nearest fill
		Head straight towards it
	}
}


Future Plans {
	Biomes {
		Biomes using different CA rules and different algorithms
		Blend between different Biomes
		Use entirely different algorithms to generate clumps of special chunks
	}

	Threading {
		Queues of thread requests with priority ordering in each queue
		A tick system where each background activity will be dispatched at its turn and has its own queue
		Control the concurrent threads dispatched at a time
	}
}